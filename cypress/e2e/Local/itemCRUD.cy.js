/// <reference types='cypress' />

describe('Item actions', () => {
  // before(() => {
  //   cy.login();
  // });

  // it('add an item', () => {
  // });

  // it('edit item', () => {
  // });

  // it('delete item', () => {
  // });

  // it('sell one item', () => {
  // });

  // it('sell max qty item', () => {
  // });

  // it('reset gold totals', () => {
  // });
});
